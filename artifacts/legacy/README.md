# Legacy artifact

`pixel_head_2346.pt` is the previous Large 32×4 head. Its local path metadata was replaced by the original backbone SHA-256. The required `mask_c=0.025` backbone is not currently in the Hugging Face collection, so this file is provenance only.

The portable reproduction trains a new head on `georgeven/songmae-large-32x1`.
